# Docker Development Configuration
OPENBADGES_SERVER_URL=http://localhost:3000
OPENBADGES_AUTH_MODE=docker
PORT=8888
VITE_PORT=7777

# Docker OpenBadges Server Configuration
OPENBADGES_AUTH_ENABLED=true

# Docker uses Basic Auth with admin:admin-user (as configured in docker-compose.yml)
OPENBADGES_BASIC_AUTH_USER=admin
OPENBADGES_BASIC_AUTH_PASS=admin-user

# API Key Authentication (alternative - matches docker-compose.yml)
OPENBADGES_API_KEY=test-api-key-123
